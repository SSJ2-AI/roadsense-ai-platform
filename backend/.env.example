# Backend environment template for Cloud Run

# Runtime
ENV=production
LOG_LEVEL=INFO
ALLOWED_ORIGINS=https://roadsense.brampton.ca,https://*.web.app,https://*.firebaseapp.com

# Auth
API_KEYS=replace-with-random-key-1,replace-with-random-key-2

# GCP
GCP_PROJECT_ID=your-gcp-project-id
# If using workload identity on Cloud Run, you do not need this path
GOOGLE_APPLICATION_CREDENTIALS=/var/secrets/google/key.json

# Data Stores
FIRESTORE_COLLECTION=detections
GCS_BUCKET=roadsense-potholes-prod
DATA_RETENTION_DAYS=90

# ML Model
YOLO_MODEL_PATH=/app/models/weights.pt
YOLO_CONFIDENCE_THRESHOLD=0.35

# API
MAX_UPLOAD_SIZE_MB=15
